Drop `.txt` keymap lists into this directory to load them into MusMem.

Formatting rules:
- One exercise per line.
- Use `prompt => command1, command2`.
- Commands support `Ctrl`, `Alt`, `Shift`, and `Super`.
- Load a file from Settings, then save it as a custom list to make it persistent.
