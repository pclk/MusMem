Drop `.txt` word lists into this directory to load them into MusMem.

Formatting rules:
- Separate entries with commas, newlines, or plain whitespace.
- Filenames become display names in Settings.
- Load a file from Settings, then save it as a custom list to make it persistent.
